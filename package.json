{
  "name": "react-required-if",
  "version": "1.0.1",
  "description": "React PropType to conditionally add `.isRequired` based on other props",
  "main": "build",
  "scripts": {
    "build": "npm run build:clean -s && npm run build:babel -s",
    "build:babel": "babel index.js --out-dir build",
    "build:clean": "rimraf ./build",
    "prepublish": "npm run build -s"
  },
  "author": "James Kyle <me@thejameskyle.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
